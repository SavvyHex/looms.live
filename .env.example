# Example environment variables for docker-compose
# Copy this to .env and fill in real values (DO NOT commit the real .env)

# Gemini / LLM API key (server-side)
GEMINI_API_KEY=

# Supabase public keys (if you use Supabase in the frontend)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: override backend URL (in docker-compose the frontend already points to http://backend:8001)
DAILY_TITLE_BACKEND_URL=http://backend:8001
